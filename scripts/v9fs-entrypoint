#!/usr/bin/env bash
set -euo pipefail

umask 022

mkdir -p /workspaces/tmp
mkdir -p /home/v9fs-test

# Default action: run the harness.
if [ "${1:-}" = "" ]; then
  exec /usr/local/bin/v9fs-run-tests
fi

exec "$@"

